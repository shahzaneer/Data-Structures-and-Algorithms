Node* reverse(Node *last){
//     Node *p,*q ,*r;
//     p = last->next;
//     q = last->next->next;
//     r = last->next->next->next;
//     p->next = last;
//     while(r->next!=last){
//         q->next = p;
//         r->next = q;
//     }
//     return p;

// }